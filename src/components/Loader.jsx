import "./loader.css";

// hey Rasmus, I tried to borrow your loader, not sure if it works

// anyway, thank you

export default function Loader() {
  return (
    <section className="loader">
      <section className="spinner"></section>
    </section>
  );
}
